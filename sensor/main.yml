---
- name: Honeydeck Sensor Updater
  hosts:
    - localhost
  become: no
  strategy: free 
  gather_facts: no 
  tasks:
    - name: Install Sensor Roles
      include_role:
        name: "{{ item }}"
      loop: "{{ sensor_roles }}"