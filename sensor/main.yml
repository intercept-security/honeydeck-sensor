---
- name: Honeydeck Sensor Updater
  hosts:
    - localhost
  become: yes
  become_method: sudo
  strategy: free 
  gather_facts: no 
  tasks:
    - name: Install Sensor Roles
      include_role:
        name: "{{ item }}"
      loop: "{{ sensor_roles }}"